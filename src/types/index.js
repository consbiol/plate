/**
 * Types entrypoint for JSDoc `import()` usage.
 * This file re-exports commonly referenced typedef names so callers can import from one place.
 */

/**
 * Terrain
 * @typedef {import('./terrain.js').TerrainEventType} TerrainEventType
 * @typedef {import('./terrain.js').TerrainRunMode} TerrainRunMode
 * @typedef {import('./terrain.js').TerrainRunContext} TerrainRunContext
 * @typedef {import('./terrain.js').TerrainRunCommand} TerrainRunCommand
 * @typedef {import('./terrain.js').PreGlacierStats} PreGlacierStats
 * @typedef {import('./terrain.js').DriftMetrics} DriftMetrics
 * @typedef {import('./terrain.js').LowlandDistanceToSeaStats} LowlandDistanceToSeaStats
 * @typedef {import('./terrain.js').CenterParameter} CenterParameter
 * @typedef {import('./terrain.js').TerrainEventPayload} TerrainEventPayload
 */

/**
 * Stats
 * @typedef {import('./stats.js').GridTypeCounts} GridTypeCounts
 * @typedef {import('./stats.js').ParametersStats} ParametersStats
 */

/**
 * Cache
 * @typedef {import('./terrainCache.js').TerrainHighFrequencyCache} TerrainHighFrequencyCache
 */

// Types-only token export for tooling.
export const __types = null;


